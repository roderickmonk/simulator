import logging

class Trader:

    def __init__(
        self,
        config: dict,
        ):
        logging.error (config.partition_config)
        pass

    def compute_orders(
        self,
        buyob,
        sellob,
    ):

        return [0, "", sellob[0][0], buyob[0][0]]
